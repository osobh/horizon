# Minimal configuration with defaults
# Only required fields are specified, others use serde defaults

listen_addr: "127.0.0.1:3000"
log_level: "warn"
max_connections: 50
enable_tls: false
# timeout_ms will use default value of 5000
