[package]
name = "tier-watch"
version = "0.1.0"
edition = "2024"
authors = ["StratoSwarm Team"]
description = "Kernel module for monitoring 5-tier memory hierarchy"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["staticlib"]

[dependencies]
# No external dependencies for kernel module

[build-dependencies]
cc = "1.0"

[dev-dependencies]
# No external dependencies needed for tests

[features]
default = []
# Enable additional debugging features
debug = []
# Enable benchmark tests
bench = []

# Profiles are defined at workspace level
